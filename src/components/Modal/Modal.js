import React from "react";
import "./Modal.css";

const Modal = (props) => {
  return (
    <div class="modal-overlay">
      <div class="modal-box">
        etsting
        <button>Close</button>
      </div>
    </div>
  );
};

export default Modal;
